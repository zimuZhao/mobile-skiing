/**
 * 通过 JDME SDK 判断 当前操作系统
 */

const state = {
  test:false
}

const getters = {}

const actions = {
  // updateAge({ commit, state }, params) {
  //   return Q.Promise((resolve, reject) => {
  //     commit(types.UPDATE_AGE, params);
  //     resolve(params)
  //   });
  // }
}

const mutations = {
  // [types.UPDATE_NAME](state) {

  // }
}

export default {
  state,
  getters,
  actions,
  mutations
}
